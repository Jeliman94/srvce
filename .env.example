# Mapy.cz API key (https://api.mapy.cz) used by src/lib/travel.ts to geocode
# addresses and compute round-trip travel time/distance for the "Hotové
# servisy" report. Without it, travel time/km show as "—".
VITE_MAPY_API_KEY=
